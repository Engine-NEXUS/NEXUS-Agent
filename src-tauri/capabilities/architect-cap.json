{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "architect-cap",
  "description": "Capabilities for the NEXUS Architecture Mapper sidebar window — event listening (progress, phase1/graph-ready, backdrop) and repo/analysis IPC commands.",
  "windows": ["architect-sidebar"],
  "permissions": [
    "core:default",
    "core:event:allow-emit",
    "core:event:allow-listen",
    "core:event:allow-unlisten"
  ]
}
