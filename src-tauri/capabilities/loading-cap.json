{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "loading-cap",
  "description": "Capabilities for the NEXUS Loading Indicator window — a transparent click-through overlay that shows the loading.json Lottie animation while NEXUS is processing a request.",
  "windows": ["loading-indicator"],
  "permissions": [
    "core:default",
    "core:event:allow-emit",
    "core:event:allow-listen",
    "core:event:allow-unlisten"
  ]
}
