# Ultron WSS Bridge sidecar dependencies.
# Install:  pip install -r requirements.txt
# Run:      uvicorn sidecar:app --host 0.0.0.0 --port 8443
#           (or behind Caddy which terminates TLS)

fastapi>=0.115,<1.0
uvicorn[standard]>=0.30,<1.0
httpx>=0.27,<1.0
websockets>=12.0,<14.0
cryptography>=42.0,<46.0
